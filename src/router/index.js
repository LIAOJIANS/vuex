import Vue from 'vue'
import Router from 'vue-router'
import Finsih from '../pages/Finish/Finsh.vue'

Vue.use(Router)

export default new Router({
  routes: [
    { path: '/' ,component: Finsih }
  ]
})
